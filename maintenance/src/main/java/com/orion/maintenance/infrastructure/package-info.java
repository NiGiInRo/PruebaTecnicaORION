/**
 * Capa de infraestructura: adaptadores hacia el mundo exterior — controllers
 * REST (web), configuración de seguridad/JWT (security) y configuración
 * general (config). No contiene reglas de negocio.
 */
package com.orion.maintenance.infrastructure;
